class Global {
    constructor() {
        this.intGrazerCount = 0;
        this.intPredCount = 0;
        this.intPlantCount = 0;
        this.intObsCount = 0;
        this.size = 0;
        this.time = 0.0;
        this.plantList = [];
        this.predList = [];
        this.grazerList = [];
        this.obsList = [];
    }

    newPlant() {
        // Create new plant and append to list
    }

    newGrazer() {
        // Create new grazer and append to list
    }

    newPredator() {
        // Create new predator and append to list
    }

    newObs() {
        // Create new obstacle and append to list
    }

    initializeWorld() {
        // Implement initializeWorld method logic here
        this.worldMatrix = new Array[this.size, this.size]
    }

    setWorld() {
        // Implement setWorld method logic here
    }
}
